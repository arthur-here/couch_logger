const usersRepository = (db) => {
  return {
    // addEvent: (event) => {
    //   return new Promise((resolve, reject) => {
    //     db
    //   });
    // }
  };
};

export default usersRepository;
